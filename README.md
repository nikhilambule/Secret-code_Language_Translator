# Secret-code_Language_Translator
Rule: if word starts from vowel add yay: apple -> appleyay         if word starts with consonant add first word at the end add ay: cat => atcay       (For conversation in secret coded language)
